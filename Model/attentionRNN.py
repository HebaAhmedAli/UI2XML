import sys
sys.path.append('../')
from keras.models import Model
from keras.layers import Input, LSTM, Dense
import Constants

# Create the attention rnn layers to use them in training the model.
def createAttentionRnn(encoderInputs,decoderInputs):
    
    
    return



# Create the attention from the trained layers to use in prediction.
def getTrainedEncoderDecoderModel(): 
    
    return